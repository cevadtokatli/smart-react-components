Real world example how the SRC router is used in both the client and the server side.
```
$ yarn install
$ yarn run build
$ yarn run start
```
Open <a href="http://localhost:8080" target="_blank">http://localhost:8080</a> to view it in the browser.