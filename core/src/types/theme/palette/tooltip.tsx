export interface TooltipPaletteItemAttributes {
  background?: string // main
  font?: string // font
  shadow?: string // shadow

  soft?: {
    background?: string // soft
    font?: string // softFont
  }
}
