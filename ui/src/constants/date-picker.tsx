export enum DatePickerSelectedDatePicker {
  WEEK = '0',
  MONTH = '1',
  YEAR = '2',
}

export enum DatePickerSelectedPicker {
  DATE = '0',
  TIME = '1',
}

export enum DatePickerTimeType {
  T12,
  T24,
}

export enum DatePickerType {
  DATE,
  TIME,
  DATE_TIME,
}
